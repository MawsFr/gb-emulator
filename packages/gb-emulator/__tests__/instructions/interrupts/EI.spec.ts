import { describe, it } from 'vitest'
import { GbEmulatorTestContext } from '$/test.setup.ts'
import { EI } from '@/instructions/interrupts/EI.ts'

describe(EI, () => {
    it<GbEmulatorTestContext>('should enable interrupts', () => {
        // Given
        // When
        // Then
    })
})
