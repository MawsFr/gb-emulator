import { describe, it } from 'vitest'
import { GbEmulatorTestContext } from '$/test.setup.ts'
import { DI } from '@/instructions/interrupts/DI.ts'

describe(DI, () => {
    it<GbEmulatorTestContext>('should disable interrupts', () => {
        // Given
        // When
        // Then
    })
})
